import { createAction } from '@ngrx/store';

export const increment = createAction('[Homepage Component] Increment');
export const decrement = createAction('[Homepage Component] Decrement');
export const reset = createAction('[Homepage Component] Reset');
